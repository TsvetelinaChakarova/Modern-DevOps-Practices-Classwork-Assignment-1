name:  Build and publish docker image

on:
  push:
    branches: [ main ]

jobs:

  Build:
    runs-on: ubuntu-latest
    steps:
    - name: Build the Docker image
    - uses: actions/checkout@v3
      run: docker build -t MDP-Classwork-Assignment-1:$(date +%s) src/
